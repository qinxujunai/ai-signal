---
description: AI Signal project setup and configuration instructions
globs: "**/*"
alwaysApply: true
---

# AI Signal — Cursor Rules

本项目是 AI 行业每日简报系统。用户克隆后，帮他们完成以下配置：

1. `cd scripts && npm install`
2. 检测 API key：环境变量 → `~/.claude/settings.json`（sk- 开头的 token）→ 用户手动提供
3. 创建 `~/.ai-signal/.env`（QQ_EMAIL、QQ_SMTP_AUTH、DEEPSEEK_API_KEY）
4. 创建 `~/.ai-signal/config.json`
5. 运行：`node prepare-digest.js --out /tmp/feed.json && node remix-digest.js --file /tmp/feed.json --out /tmp/digest.html && node deliver.js --file /tmp/digest.html --force`

配置文件位置：`~/.ai-signal/`
调度脚本：`scripts/run-digest.sh`
